<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Foundation — CTRC Club Manager</title>
  <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <style>
    @font-face { font-family: 'Inter'; font-style: normal; font-weight: 400; font-display: block; src: url('fonts/inter-400.woff2') format('woff2'); }
    @font-face { font-family: 'Inter'; font-style: normal; font-weight: 500; font-display: block; src: url('fonts/inter-500.woff2') format('woff2'); }
    @font-face { font-family: 'Inter'; font-style: normal; font-weight: 600; font-display: block; src: url('fonts/inter-600.woff2') format('woff2'); }
    @font-face { font-family: 'Inter'; font-style: normal; font-weight: 700; font-display: block; src: url('fonts/inter-700.woff2') format('woff2'); }
    @font-face { font-family: 'Inter'; font-style: normal; font-weight: 800; font-display: block; src: url('fonts/inter-800.woff2') format('woff2'); }
    @font-face { font-family: 'Inter'; font-style: normal; font-weight: 900; font-display: block; src: url('fonts/inter-900.woff2') format('woff2'); }
  </style>
  <style>
    :root {
      --yellow:#F5D000;--yellow-dim:#C8AA00;--yellow-bg:#FFFBDC;
      --black:#171717;--black-2:#262626;--black-3:#404040;
      --gray-1:#737373;--gray-2:#A3A3A3;--gray-3:#D4D4D4;--gray-4:#F5F5F5;
      --white:#FFFFFF;--sidebar-w:240px;--radius:10px;--radius-sm:6px;
      --shadow:0 1px 3px rgba(0,0,0,.08);--shadow-md:0 4px 12px rgba(0,0,0,.10);
    }
    *,*::before,*::after{box-sizing:border-box;margin:0;padding:0;}
    body{font-family:'Inter',sans-serif;background:var(--gray-4);color:var(--black);min-height:100vh;display:flex;}

    /* SIDEBAR */
    .sidebar{width:var(--sidebar-w);background:var(--black);display:flex;flex-direction:column;position:fixed;top:0;left:0;bottom:0;z-index:100;}
    .sidebar-logo{padding:20px 18px 16px;border-bottom:1px solid var(--black-2);}
    .sidebar-logo img{width:140px;display:block;}
    .sidebar-logo-sub{font-size:10px;color:var(--gray-2);letter-spacing:.08em;text-transform:uppercase;margin-top:6px;}
    .sidebar-nav{flex:1;padding:12px 10px;display:flex;flex-direction:column;gap:2px;overflow-y:auto;}
    .nav-section-label{font-size:10px;font-weight:600;color:var(--gray-1);letter-spacing:.1em;text-transform:uppercase;padding:10px 8px 4px;}
    .nav-item{display:flex;align-items:center;gap:10px;padding:8px 10px;border-radius:var(--radius-sm);color:var(--gray-2);font-size:13.5px;font-weight:500;cursor:pointer;transition:background .15s,color .15s;text-decoration:none;}
    .nav-item:hover{background:var(--black-2);color:var(--white);}
    .nav-item.active{background:var(--yellow);color:var(--black);font-weight:700;}
    .nav-item i{width:16px;height:16px;}
    .nav-badge{margin-left:auto;background:var(--yellow);color:var(--black);font-size:10px;font-weight:700;padding:1px 6px;border-radius:99px;}
    .nav-item.active .nav-badge{background:var(--black);color:var(--yellow);}
    .sidebar-footer{padding:14px 14px;border-top:1px solid var(--black-3);}
    .sidebar-user{display:flex;align-items:center;gap:10px;cursor:pointer;}
    .sidebar-avatar{width:34px;height:34px;border-radius:50%;background:var(--yellow);display:flex;align-items:center;justify-content:center;overflow:hidden;flex-shrink:0;}
    .sidebar-avatar img{width:100%;height:100%;object-fit:cover;}
    .sidebar-user-info{flex:1;min-width:0;}
    .sidebar-user-name{font-size:13px;font-weight:600;color:var(--white);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
    .sidebar-user-role{font-size:11px;color:var(--gray-2);}

    /* MAIN */
    .main{margin-left:var(--sidebar-w);flex:1;min-width:0;overflow-x:hidden;display:flex;flex-direction:column;min-height:100vh;}

    /* TOPBAR */
    .topbar{background:var(--white);border-bottom:1px solid var(--gray-3);padding:0 28px;height:56px;display:flex;align-items:center;gap:12px;position:sticky;top:0;z-index:50;}
    .breadcrumb{display:flex;align-items:center;gap:6px;font-size:13px;}
    .breadcrumb a{color:var(--gray-1);text-decoration:none;font-weight:500;}
    .breadcrumb a:hover{color:var(--black);}
    .breadcrumb-sep{color:var(--gray-3);}
    .breadcrumb-cur{color:var(--black);font-weight:600;}
    .topbar-right{margin-left:auto;display:flex;align-items:center;gap:8px;}
    .btn{display:inline-flex;align-items:center;gap:6px;font-size:13px;font-weight:600;padding:7px 14px;border-radius:var(--radius-sm);border:none;cursor:pointer;transition:opacity .15s,transform .1s;white-space:nowrap;}
    .btn:active{transform:scale(.97);}
    .btn-yellow{background:var(--yellow);color:var(--black);}
    .btn-yellow:hover{opacity:.88;}
    .btn-outline{background:transparent;border:1.5px solid var(--gray-3);color:var(--black-3);}
    .btn-outline:hover{border-color:var(--gray-2);}
    .icon-btn{width:34px;height:34px;border-radius:var(--radius-sm);display:flex;align-items:center;justify-content:center;border:1.5px solid var(--gray-3);background:var(--white);cursor:pointer;color:var(--gray-1);transition:all .15s;position:relative;}
    .notif-dot{position:absolute;top:5px;right:5px;width:7px;height:7px;background:var(--yellow);border-radius:50%;border:1.5px solid var(--white);}

    /* CONTENT */
    .content{padding:28px;display:flex;flex-direction:column;gap:20px;}
    .page-header{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;flex-wrap:wrap;}
    .page-title{font-size:22px;font-weight:900;color:var(--black);letter-spacing:-.02em;}
    .page-subtitle{font-size:13px;color:var(--gray-1);margin-top:3px;}

    /* STAT CARDS */
    .stat-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;}
    .stat-card{background:var(--white);border:1px solid var(--gray-3);border-radius:var(--radius);padding:16px 18px;box-shadow:var(--shadow);position:relative;overflow:hidden;}
    .stat-card.accent-yellow{border-top:3px solid var(--yellow);}
    .stat-card.accent-red{border-top:3px solid #EF4444;}
    .stat-card.accent-green{border-top:3px solid #22C55E;}
    .stat-card.accent-black{border-top:3px solid var(--black);}
    .stat-label{font-size:10.5px;font-weight:700;color:var(--gray-1);text-transform:uppercase;letter-spacing:.07em;}
    .stat-value{font-size:28px;font-weight:900;color:var(--black);letter-spacing:-.03em;line-height:1;margin-top:6px;}
    .stat-meta{font-size:11.5px;color:var(--gray-2);margin-top:4px;}
    .stat-icon{position:absolute;top:14px;right:14px;width:30px;height:30px;background:var(--gray-4);border-radius:var(--radius-sm);display:flex;align-items:center;justify-content:center;color:var(--gray-2);}

    /* FILTER BAR */
    .filter-bar{display:flex;align-items:center;gap:8px;flex-wrap:wrap;}
    .filter-chip{display:inline-flex;align-items:center;gap:6px;padding:6px 12px;border-radius:var(--radius-sm);border:1.5px solid var(--gray-3);background:var(--white);font-size:12.5px;font-weight:500;color:var(--black-3);cursor:pointer;}
    .filter-chip select{border:none;background:transparent;font-size:12.5px;font-weight:600;color:inherit;cursor:pointer;outline:none;font-family:'Inter',sans-serif;appearance:none;}
    .filter-chip-label{font-size:10px;font-weight:700;color:var(--gray-1);text-transform:uppercase;letter-spacing:.06em;}
    .search-chip{flex:1;max-width:260px;display:flex;align-items:center;gap:8px;padding:6px 12px;border-radius:var(--radius-sm);border:1.5px solid var(--gray-3);background:var(--white);}
    .search-chip input{border:none;background:transparent;font-size:12.5px;outline:none;width:100%;font-family:'Inter',sans-serif;color:var(--black);}
    .stalled-toggle{display:inline-flex;align-items:center;gap:6px;padding:6px 12px;border-radius:var(--radius-sm);border:1.5px solid var(--gray-3);background:var(--white);font-size:12px;font-weight:600;color:var(--gray-1);cursor:pointer;transition:all .15s;user-select:none;}
    .stalled-toggle.active{border-color:#EF4444;background:#FEF2F2;color:#DC2626;}
    .stalled-toggle i{width:13px;height:13px;}

    /* TABLE */
    .card{background:var(--white);border:1px solid var(--gray-3);border-radius:var(--radius);box-shadow:var(--shadow);overflow:hidden;}
    .card-header{display:flex;align-items:center;justify-content:space-between;padding:14px 20px;border-bottom:1px solid var(--gray-3);}
    .card-title{font-size:14px;font-weight:700;color:var(--black);}
    .table-wrap{overflow-x:auto;}
    table{width:100%;border-collapse:collapse;}
    thead th{font-size:11px;font-weight:700;color:var(--gray-1);letter-spacing:.06em;text-transform:uppercase;text-align:left;padding:10px 18px;border-bottom:1px solid var(--gray-3);background:var(--gray-4);white-space:nowrap;cursor:pointer;}
    thead th:hover{color:var(--black);}
    thead th.sorted{color:var(--black);}

    tbody tr.student-row{border-bottom:1px solid var(--gray-3);cursor:pointer;transition:background .1s;}
    tbody tr.student-row:hover{background:var(--yellow-bg);}
    tbody tr.student-row.expanded{background:var(--yellow-bg);}
    tbody tr.student-row:last-of-type{border-bottom:1px solid var(--gray-3);}
    td{padding:13px 18px;font-size:13px;color:var(--black-3);vertical-align:middle;}

    /* student cell */
    .student-cell{display:flex;align-items:center;gap:11px;}
    .avatar{width:36px;height:36px;border-radius:50%;font-size:12px;font-weight:700;display:flex;align-items:center;justify-content:center;flex-shrink:0;box-shadow:0 0 0 1.5px var(--gray-3);}
    .s-name{font-size:13.5px;font-weight:700;color:var(--black);}
    .s-email{font-size:11px;color:var(--gray-2);margin-top:1px;}

    /* week badge */
    .week-badge{display:inline-flex;align-items:center;justify-content:center;padding:3px 10px;border-radius:99px;font-size:11.5px;font-weight:800;white-space:nowrap;}
    .week-1,.week-2{background:#EFF6FF;color:#1D4ED8;}
    .week-3,.week-4{background:#F5F3FF;color:#5B21B6;}
    .week-5,.week-6{background:var(--yellow-bg);color:#92700A;}
    .week-7,.week-8{background:#F0FDF4;color:#15803D;}
    .week-done{background:var(--black);color:var(--yellow);}

    /* 8-segment progress bar */
    .seg-bar{display:flex;gap:2px;align-items:center;}
    .seg{width:16px;height:8px;border-radius:2px;}
    .seg.done{background:var(--yellow);}
    .seg.current{background:var(--yellow-dim);outline:2px solid var(--yellow-dim);outline-offset:1px;}
    .seg.empty{background:var(--gray-3);}

    /* status badge */
    .badge{display:inline-flex;align-items:center;gap:4px;font-size:10.5px;font-weight:700;padding:2px 8px;border-radius:4px;}
    .badge-green{background:#F0FDF4;color:#15803D;}
    .badge-yellow{background:var(--yellow-bg);color:#92700A;}
    .badge-red{background:#FEF2F2;color:#DC2626;}
    .badge-blue{background:#EFF6FF;color:#1D4ED8;}
    .badge-gray{background:var(--gray-3);color:var(--black-3);}

    /* actions */
    .row-actions{display:flex;align-items:center;gap:6px;}
    .expand-btn{width:28px;height:28px;border-radius:var(--radius-sm);border:1.5px solid var(--gray-3);background:transparent;display:flex;align-items:center;justify-content:center;cursor:pointer;transition:all .15s;color:var(--gray-1);}
    .expand-btn:hover{border-color:var(--black);color:var(--black);}
    .expand-btn.open{background:var(--yellow);border-color:var(--yellow);color:var(--black);}
    .pdf-btn{display:inline-flex;align-items:center;gap:4px;font-size:11.5px;font-weight:700;padding:4px 10px;border-radius:var(--radius-sm);border:1.5px solid var(--gray-3);background:transparent;color:var(--black-3);cursor:pointer;white-space:nowrap;transition:all .15s;}
    .pdf-btn:hover{background:var(--yellow);border-color:var(--yellow);color:var(--black);}

    /* EXPANDED PANEL */
    .expand-row td{padding:0;border-bottom:1px solid var(--gray-3);}
    .expand-panel{background:#FAFAFA;border-top:2px solid var(--yellow);}
    .panel-inner{display:grid;grid-template-columns:1fr 290px;gap:0;}
    .panel-left{padding:22px 24px;border-right:1px solid var(--gray-3);}
    .panel-right{padding:20px;display:flex;flex-direction:column;gap:16px;}
    .panel-section-title{font-size:11px;font-weight:700;color:var(--gray-1);text-transform:uppercase;letter-spacing:.08em;margin-bottom:12px;}

    /* per-student curriculum timeline */
    .student-timeline{display:flex;gap:0;position:relative;margin-bottom:24px;}
    .stl-item{display:flex;flex-direction:column;align-items:center;flex:1;position:relative;}
    .stl-connector{position:absolute;top:13px;left:50%;right:-50%;height:3px;z-index:0;}
    .stl-connector.done{background:var(--yellow);}
    .stl-connector.upcoming{background:var(--gray-3);}
    .stl-item:last-child .stl-connector{display:none;}
    .stl-node{width:26px;height:26px;border-radius:50%;display:flex;align-items:center;justify-content:center;z-index:1;font-size:10px;font-weight:800;flex-shrink:0;border:2px solid transparent;}
    .stl-done{background:var(--yellow);color:var(--black);}
    .stl-current{background:var(--yellow);color:var(--black);box-shadow:0 0 0 3px var(--yellow-bg),0 0 0 5px var(--yellow-dim);}
    .stl-upcoming{background:var(--white);border-color:var(--gray-3);color:var(--gray-2);}
    .stl-label{font-size:9.5px;color:var(--gray-1);text-align:center;margin-top:7px;max-width:68px;line-height:1.35;}
    .stl-label.current{color:var(--black);font-weight:700;}
    .stl-date{font-size:9px;color:var(--gray-2);margin-top:2px;}
    .stl-date.current{color:var(--yellow-dim);font-weight:700;}

    /* week log list */
    .week-log{display:flex;flex-direction:column;gap:8px;}
    .week-log-item{background:var(--white);border:1px solid var(--gray-3);border-radius:var(--radius-sm);padding:12px 14px;display:flex;gap:12px;align-items:flex-start;}
    .week-log-item.done{border-left:3px solid var(--yellow);}
    .week-log-item.current{border-left:3px solid var(--black);background:var(--yellow-bg);}
    .week-log-item.upcoming{opacity:.45;}
    .wl-num{font-size:10px;font-weight:800;color:var(--gray-1);text-transform:uppercase;letter-spacing:.06em;min-width:48px;}
    .wl-topic{font-size:12.5px;font-weight:700;color:var(--black);}
    .wl-meta{font-size:11px;color:var(--gray-1);margin-top:2px;}
    .wl-actions{margin-left:auto;flex-shrink:0;}
    .mark-done-btn{font-size:11px;font-weight:700;padding:4px 10px;border-radius:var(--radius-sm);border:1.5px solid var(--gray-3);background:var(--white);cursor:pointer;color:var(--black-3);white-space:nowrap;transition:all .15s;}
    .mark-done-btn:hover{background:var(--yellow);border-color:var(--yellow);color:var(--black);}

    /* photos */
    .photo-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-bottom:20px;}
    .photo-card{aspect-ratio:1;border-radius:var(--radius-sm);display:flex;flex-direction:column;align-items:center;justify-content:center;gap:5px;font-size:10px;font-weight:600;color:var(--gray-1);border:1.5px dashed var(--gray-3);background:var(--gray-4);cursor:pointer;transition:all .15s;text-align:center;padding:8px;}
    .photo-card:hover{border-color:var(--yellow);background:var(--yellow-bg);}
    .photo-card.filled{border:none;background:var(--black-2);color:var(--white);border-radius:var(--radius-sm);}
    .photo-label{font-size:9.5px;font-weight:600;color:var(--gray-3);margin-top:3px;}

    /* notes */
    .note-item{display:flex;gap:10px;padding:10px 0;border-bottom:1px solid var(--gray-3);}
    .note-item:last-of-type{border-bottom:none;}
    .note-avt{width:26px;height:26px;border-radius:50%;background:var(--black);display:flex;align-items:center;justify-content:center;font-size:9px;font-weight:700;color:var(--yellow);flex-shrink:0;}
    .note-author{font-size:12px;font-weight:700;color:var(--black);}
    .note-date{font-size:10.5px;color:var(--gray-2);float:right;}
    .note-text{font-size:12.5px;color:var(--black-3);line-height:1.55;margin-top:3px;}
    .note-add{display:flex;gap:8px;margin-top:10px;}
    .note-add textarea{flex:1;border:1.5px solid var(--gray-3);border-radius:var(--radius-sm);padding:7px 10px;font-size:12.5px;font-family:inherit;resize:none;outline:none;background:var(--white);}
    .note-add textarea:focus{border-color:var(--yellow);}

    /* right panel */
    .mini-stat-card{background:var(--white);border:1px solid var(--gray-3);border-radius:var(--radius-sm);padding:12px 14px;}
    .ms-row{display:flex;justify-content:space-between;align-items:center;padding:5px 0;border-bottom:1px solid var(--gray-3);font-size:12.5px;}
    .ms-row:last-child{border-bottom:none;}
    .ms-label{color:var(--gray-1);}
    .ms-val{font-weight:700;color:var(--black);}
    .score-table{width:100%;border-collapse:collapse;font-size:12px;}
    .score-table th{font-size:10px;font-weight:700;color:var(--gray-1);text-transform:uppercase;letter-spacing:.05em;padding:5px 0;border-bottom:1px solid var(--gray-3);text-align:left;}
    .score-table td{padding:5px 0;border-bottom:1px solid var(--gray-3);color:var(--black-3);}
    .score-table tr:last-child td{border-bottom:none;}
    .parent-report-card{background:var(--yellow-bg);border:1.5px solid var(--yellow);border-radius:var(--radius-sm);padding:14px;}
    .pr-title{font-size:13px;font-weight:700;color:var(--black);margin-bottom:4px;}
    .pr-desc{font-size:11.5px;color:var(--black-3);line-height:1.5;margin-bottom:12px;}
    .pr-last{font-size:11px;color:var(--gray-1);margin-top:6px;text-align:center;}

    /* caution strip */
    .caution-strip{height:5px;background:repeating-linear-gradient(-55deg,var(--yellow) 0,var(--yellow) 10px,var(--black) 10px,var(--black) 20px);opacity:.15;}

    /* PRINT */
    @media print {
      .sidebar,.topbar,.filter-bar,.card,.stat-grid,.page-header{display:none!important;}
      #print-area{display:block!important;}
      body{background:white;}
      .print-header{display:flex;align-items:center;gap:16px;margin-bottom:24px;padding-bottom:16px;border-bottom:3px solid #F5D000;}
      .print-logo{font-size:18px;font-weight:900;letter-spacing:-.02em;}
      .print-logo span{color:#F5D000;}
      .print-section{margin-bottom:20px;}
      .print-section-title{font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.08em;color:#737373;border-bottom:1px solid #D4D4D4;padding-bottom:4px;margin-bottom:10px;}
      .print-row{display:flex;gap:32px;margin-bottom:6px;font-size:12px;}
      .print-label{color:#737373;min-width:120px;}
      .print-val{font-weight:600;}
      .print-week-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:6px;}
      .print-week-cell{border:1px solid #D4D4D4;border-radius:4px;padding:8px;font-size:11px;}
      .print-week-cell.done{background:#FFFBDC;border-color:#F5D000;}
      .print-score-table{width:100%;border-collapse:collapse;font-size:12px;}
      .print-score-table th{background:#F5F5F5;text-align:left;padding:5px 8px;font-size:10px;text-transform:uppercase;}
      .print-score-table td{padding:5px 8px;border-bottom:1px solid #D4D4D4;}
      .print-footer{margin-top:32px;padding-top:12px;border-top:1px solid #D4D4D4;font-size:10px;color:#A3A3A3;display:flex;justify-content:space-between;}
    }
    #print-area{display:none;}
  </style>
</head>
<body>

<!-- SIDEBAR -->
<aside class="sidebar">
  <div class="sidebar-logo">
    <img src="logo-white.png" alt="Caution Tape Robotics" />
    <div class="sidebar-logo-sub">Club Manager</div>
  </div>
  <nav class="sidebar-nav">
    <div class="nav-section-label">General</div>
    <a class="nav-item" href="index.html"><i data-lucide="layout-dashboard"></i> Dashboard</a>
    <a class="nav-item" href="students.html"><i data-lucide="users"></i> Students</a>
    <a class="nav-item active" href="foundation.html"><i data-lucide="book-open"></i> Foundation</a>
    <a class="nav-item" href="teams.html"><i data-lucide="cpu"></i> Teams</a>
    <a class="nav-item" href="tasks.html"><i data-lucide="circle-check"></i> Tasks <span class="nav-badge">5</span></a>
    <a class="nav-item" href="projects.html"><i data-lucide="folder"></i> Projects</a>
    <div class="nav-section-label" style="margin-top:8px;">Competition</div>
    <a class="nav-item" href="https://matchsight.ct839.com/events?programId=1" target="_blank"><i data-lucide="trophy"></i> Competitions</a>
    <div class="nav-section-label" style="margin-top:8px;">Operations</div>
    <a class="nav-item" href="inventory.html"><i data-lucide="package"></i> Inventory</a>
    <a class="nav-item" href="purchases.html"><i data-lucide="receipt"></i> Purchases</a>
    <a class="nav-item" href="purchases-invoices.html"><i data-lucide="file-text"></i> Invoices</a>
    <a class="nav-item" href="reports.html"><i data-lucide="bar-chart-2"></i> Reports</a>
    <div style="margin-top:auto;"></div>
    <a class="nav-item" href="#"><i data-lucide="settings"></i> Settings</a>
  </nav>
  <div class="sidebar-footer">
    <div class="sidebar-user">
      <div class="sidebar-avatar"><img src="icon-white.png" alt="User" /></div>
      <div style="flex:1;min-width:0;">
        <div class="sidebar-user-name">Malhar Soni</div>
        <div class="sidebar-user-role">Head Coach</div>
      </div>
      <i data-lucide="chevron-up" style="width:14px;height:14px;color:#737373;flex-shrink:0;"></i>
    </div>
  </div>
</aside>

<!-- MAIN -->
<div class="main">
  <header class="topbar">
    <div class="breadcrumb">
      <a href="index.html">Dashboard</a>
      <span class="breadcrumb-sep"><i data-lucide="chevron-right" style="width:13px;height:13px;"></i></span>
      <span class="breadcrumb-cur">Foundation</span>
    </div>
    <div class="topbar-right">
      <select style="border:1.5px solid var(--gray-3);border-radius:var(--radius-sm);padding:6px 12px;font-size:12.5px;font-family:'Inter',sans-serif;font-weight:600;background:var(--white);color:var(--black);cursor:pointer;outline:none;">
        <option>Fall 2025</option><option>Spring 2025</option><option>Fall 2024</option>
      </select>
      <button class="btn btn-yellow" onclick="openEnrollModal()">
        <i data-lucide="user-plus" style="width:13px;height:13px;"></i> Enroll Student
      </button>
      <div class="icon-btn" onclick="openNotifPanel()" style="position:relative;cursor:pointer;"><i data-lucide="bell" style="width:15px;height:15px;"></i><span class="notif-dot"></span></div>
    </div>
  </header>

  <main class="content">

    <!-- PAGE HEADER -->
    <div class="page-header">
      <div>
        <div class="page-title">Foundation Program</div>
        <div class="page-subtitle">8-week onboarding curriculum · Each student progresses at their own pace · Fall 2025</div>
      </div>
    </div>

    <!-- STAT CARDS -->
    <div class="stat-grid">
      <div class="stat-card accent-black">
        <div class="stat-icon"><i data-lucide="users" style="width:15px;height:15px;"></i></div>
        <div class="stat-label">Enrolled</div>
        <div class="stat-value">7</div>
        <div class="stat-meta">Active this cohort</div>
      </div>
      <div class="stat-card accent-yellow">
        <div class="stat-icon"><i data-lucide="trending-up" style="width:15px;height:15px;"></i></div>
        <div class="stat-label">Avg Week</div>
        <div class="stat-value">5.1</div>
        <div class="stat-meta">Across all students</div>
      </div>
      <div class="stat-card accent-green">
        <div class="stat-icon"><i data-lucide="graduation-cap" style="width:15px;height:15px;"></i></div>
        <div class="stat-label">Graduated</div>
        <div class="stat-value">0</div>
        <div class="stat-meta">Completed all 8 weeks</div>
      </div>
      <div class="stat-card accent-red">
        <div class="stat-icon"><i data-lucide="clock" style="width:15px;height:15px;"></i></div>
        <div class="stat-label">Stalled</div>
        <div class="stat-value" style="color:#DC2626;">1</div>
        <div class="stat-meta">No progress in 14+ days</div>
      </div>
    </div>

    <!-- FILTER BAR -->
    <div class="filter-bar">
      <div class="search-chip">
        <i data-lucide="search" style="width:13px;height:13px;color:var(--gray-2);flex-shrink:0;"></i>
        <input type="text" id="search-input" placeholder="Search by name…" oninput="applyFilters()" />
      </div>
      <div class="filter-chip">
        <span class="filter-chip-label">Status</span>
        <select id="filter-status" onchange="applyFilters()">
          <option value="">All</option>
          <option value="Excellent">Excellent</option>
          <option value="On Track">On Track</option>
          <option value="Needs Attention">Needs Attention</option>
        </select>
        <i data-lucide="chevron-down" style="width:12px;height:12px;"></i>
      </div>
      <div class="filter-chip">
        <span class="filter-chip-label">Week</span>
        <select id="filter-week" onchange="applyFilters()">
          <option value="">All Weeks</option>
          <option value="1">Week 1</option><option value="2">Week 2</option>
          <option value="3">Week 3</option><option value="4">Week 4</option>
          <option value="5">Week 5</option><option value="6">Week 6</option>
          <option value="7">Week 7</option><option value="8">Week 8</option>
        </select>
        <i data-lucide="chevron-down" style="width:12px;height:12px;"></i>
      </div>
      <div class="stalled-toggle" id="stalled-btn" onclick="toggleStalled()">
        <i data-lucide="alert-circle"></i> Stalled Only
      </div>
    </div>

    <!-- TABLE -->
    <div class="card">
      <div class="card-header">
        <div class="card-title">Students <span id="row-count" style="font-size:12px;font-weight:400;color:var(--gray-1);margin-left:6px;"></span></div>
        <span style="font-size:12px;color:var(--gray-1);">Click a row to view individual progress</span>
      </div>
      <div class="table-wrap">
        <table>
          <thead>
            <tr>
              <th onclick="sortBy('name')">Student <span id="si-name"></span></th>
              <th onclick="sortBy('week')" class="sorted">Current Week <span id="si-week">↓</span></th>
              <th>Progress</th>
              <th onclick="sortBy('pct')">Completion <span id="si-pct"></span></th>
              <th>Current Topic</th>
              <th>Status</th>
              <th onclick="sortBy('lastSeen')">Last Activity <span id="si-lastSeen"></span></th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody id="foundation-tbody"></tbody>
        </table>
      </div>
      <div class="caution-strip"></div>
    </div>

  </main>
</div>

<div id="print-area"></div>

<script>
lucide.createIcons();

const WEEKS_LIST = [
  'VEX IQ Intro & Safety',
  'Basic Assembly & Mechanisms',
  'Programming Fundamentals',
  'Sensors & Automation',
  'Drive Base Design',
  'Subsystem Integration',
  'Autonomous Programming',
  'Final Project Showcase'
];

const STUDENTS = [
  { id:1,  name:'Luke Fu',        email:'luke.fu@ctrc.club',        initials:'LF', bg:'#DBEAFE', tc:'#1D4ED8',
    week:6, pct:88, status:'On Track',        lastSeen:'2 days ago', stalled:false,
    weekDates:['Jan 11','Jan 18','Jan 25','Feb 1','Feb 8','Feb 15',null,null],
    attendance:[1,1,1,1,1,1,0,0],
    scores:[{w:1,s:9,n:'Great attitude'},{w:2,s:8,n:'Solid assembly'},{w:3,s:9,n:'Picked up coding'},{w:4,s:8,n:'Good sensor work'},{w:5,s:9,n:'Clean design'},{w:6,s:8,n:'Integration solid'}],
    photos:3,
    notes:[{i:'MS',a:'Soni',t:'Feb 15',txt:'Excellent effort on drive base. Staying late to fix gear mesh.'}]
  },
  { id:2,  name:'Brayden Sun',    email:'brayden.sun@ctrc.club',    initials:'BU', bg:'#FEF3C7', tc:'#92400E',
    week:6, pct:91, status:'Excellent',       lastSeen:'Today',      stalled:false,
    weekDates:['Jan 11','Jan 18','Jan 25','Feb 1','Feb 8','Feb 15',null,null],
    attendance:[1,1,1,1,1,1,0,0],
    scores:[{w:1,s:9,n:'Strong start'},{w:2,s:9,n:'Good teamwork'},{w:3,s:9,n:'Clean code'},{w:4,s:10,n:'Perfect sensors'},{w:5,s:9,n:'Solid build'},{w:6,s:9,n:'Consistent'}],
    photos:3,
    notes:[{i:'MS',a:'Soni',t:'Feb 15',txt:'Brayden is reliable. Good candidate for team captain role.'}]
  },
  { id:3,  name:'Keegan Ramsaran', email:'keegan.ramsaran@ctrc.club', initials:'KR', bg:'#EDE9FE', tc:'#5B21B6',
    week:4, pct:61, status:'Needs Attention', lastSeen:'2 weeks ago',stalled:true,
    weekDates:['Jan 11','Jan 18',null,'Feb 1',null,null,null,null],
    attendance:[1,1,0,1,0,0,0,0],
    scores:[{w:1,s:6,n:'Quiet first day'},{w:2,s:7,n:'Assembly OK'},{w:3,s:6,n:'Struggling with syntax'},{w:4,s:6,n:'Sensor lab incomplete'}],
    photos:0,
    notes:[{i:'MS',a:'Soni',t:'Feb 8',txt:'Keegan has missed 2 sessions. Parent contact attempted.'},{i:'US',a:'Sajjad',t:'Feb 1',txt:'Behind on sensor assignment — extended deadline.'}]
  },
  { id:4,  name:'Bryan Kuan',     email:'bryan.kuan@ctrc.club',     initials:'BK', bg:'#ECFDF5', tc:'#065F46',
    week:5, pct:78, status:'On Track',        lastSeen:'5 days ago', stalled:false,
    weekDates:['Jan 11','Jan 18','Jan 25','Feb 1','Feb 8',null,null,null],
    attendance:[1,1,1,0,1,0,0,0],
    scores:[{w:1,s:8,n:'Good start'},{w:2,s:8,n:'Steady'},{w:3,s:7,n:'Code review needed'},{w:4,s:8,n:'Improved'},{w:5,s:8,n:'Drive base solid'}],
    photos:2,
    notes:[{i:'EY',a:'Yu',t:'Feb 10',txt:'Bryan is on track. Focus on code quality next session.'}]
  },
  { id:5,  name:'Elyse To',       email:'elyse.to@ctrc.club',       initials:'ET', bg:'#FFF7ED', tc:'#9A3412',
    week:6, pct:93, status:'Excellent',       lastSeen:'Today',      stalled:false,
    weekDates:['Jan 11','Jan 18','Jan 25','Feb 1','Feb 8','Feb 15',null,null],
    attendance:[1,1,1,1,1,1,0,0],
    scores:[{w:1,s:9,n:'Excellent start'},{w:2,s:10,n:'Fastest builder'},{w:3,s:9,n:'Great code structure'},{w:4,s:9,n:'Precise'},{w:5,s:10,n:'Best drive base'},{w:6,s:9,n:'Ahead of schedule'}],
    photos:4,
    notes:[{i:'MS',a:'Soni',t:'Feb 15',txt:'Elyse helped two students debug their code this week. Exceptional.'}]
  },
  { id:6,  name:'Jayden Yang',      email:'jayden.yang@ctrc.club',      initials:'JY', bg:'#F0FDFA', tc:'#0F766E',
    week:6, pct:88, status:'On Track',        lastSeen:'2 days ago', stalled:false,
    weekDates:['Jan 11','Jan 18','Jan 25','Feb 1','Feb 8','Feb 15',null,null],
    attendance:[1,1,1,1,1,1,0,0],
    scores:[{w:1,s:9,n:'Strong intro'},{w:2,s:8,n:'Good teamwork'},{w:3,s:9,n:'Excellent comments'},{w:4,s:8,n:'Clean sensor work'},{w:5,s:9,n:'Nice CAD layout'},{w:6,s:8,n:'Good progress'}],
    photos:3,
    notes:[{i:'MS',a:'Soni',t:'Feb 15',txt:'Jayden is making steady progress.'}]
  },
  { id:7,  name:'Thomas Yang',    email:'thomas.yang@ctrc.club',    initials:'TY', bg:'#F0FDF4', tc:'#15803D',
    week:5, pct:78, status:'On Track',        lastSeen:'5 days ago', stalled:false,
    weekDates:['Jan 11','Jan 18','Jan 25','Feb 1','Feb 8',null,null,null],
    attendance:[1,1,1,0,1,0,0,0],
    scores:[{w:1,s:8,n:'Good start'},{w:2,s:8,n:'Steady'},{w:3,s:7,n:'Code review needed'},{w:4,s:8,n:'Improved'},{w:5,s:8,n:'Drive base solid'}],
    photos:2,
    notes:[{i:'EY',a:'Yu',t:'Feb 10',txt:'Thomas is on track. Focus on code quality next session.'}]
  },
];

let currentSort = { key:'week', dir:'desc' };
let showStalled = false;
let filtered = [...STUDENTS];
let openRow = null;

function statusBadge(s) {
  if (s === 'Excellent')       return '<span class="badge badge-green">Excellent</span>';
  if (s === 'On Track')        return '<span class="badge badge-blue">On Track</span>';
  if (s === 'Needs Attention') return '<span class="badge badge-red">Needs Attention</span>';
  return '<span class="badge badge-gray">' + s + '</span>';
}

function segBar(week, total=8) {
  let html = '<div class="seg-bar">';
  for (let i=1;i<=total;i++) {
    if (i < week)       html += '<div class="seg done"></div>';
    else if (i === week) html += '<div class="seg current"></div>';
    else                html += '<div class="seg empty"></div>';
  }
  html += '</div>';
  return html;
}

function weekBadgeClass(w) {
  if (w >= 8) return 'week-badge week-done';
  if (w >= 7) return 'week-badge week-7';
  if (w >= 5) return 'week-badge week-5';
  if (w >= 3) return 'week-badge week-3';
  return 'week-badge week-1';
}

function pctColor(p) { return p>=90?'#15803D':p>=70?'#D97706':'#DC2626'; }

function renderTable() {
  const tbody = document.getElementById('foundation-tbody');
  document.getElementById('row-count').textContent = filtered.length + ' students';
  tbody.innerHTML = '';

  filtered.forEach(s => {
    const tr = document.createElement('tr');
    tr.className = 'student-row';
    tr.id = 'row-' + s.id;
    tr.onclick = () => toggleExpand(s.id);
    tr.innerHTML = `
      <td><div class="student-cell">
        <div class="avatar" style="background:${s.bg};color:${s.tc};">${s.initials}</div>
        <div><div class="s-name">${s.name}</div><div class="s-email">${s.email}</div></div>
      </div></td>
      <td><span class="${weekBadgeClass(s.week)}">Week ${s.week}</span></td>
      <td>${segBar(s.week)}</td>
      <td><span style="font-weight:700;color:${pctColor(s.pct)};">${s.pct}%</span></td>
      <td style="font-size:12.5px;color:var(--black-3);max-width:160px;">${WEEKS_LIST[s.week-1]}</td>
      <td>${statusBadge(s.status)}</td>
      <td style="font-size:12.5px;color:${s.stalled?'#DC2626':'var(--gray-1)'};">${s.lastSeen}${s.stalled?' <span style="font-size:10px;font-weight:700;color:#DC2626;margin-left:4px;">STALLED</span>':''}</td>
      <td><div class="row-actions">
        <button class="expand-btn" id="btn-${s.id}"><i data-lucide="chevron-down" style="width:13px;height:13px;"></i></button>
        <button class="pdf-btn" onclick="event.stopPropagation();downloadReport(${s.id})"><i data-lucide="file-text" style="width:12px;height:12px;"></i> PDF</button>
      </div></td>`;
    tbody.appendChild(tr);

    const er = document.createElement('tr');
    er.className = 'expand-row';
    er.id = 'expand-' + s.id;
    er.style.display = 'none';
    er.innerHTML = `<td colspan="8"><div class="expand-panel"><div class="panel-inner">${buildLeft(s)}${buildRight(s)}</div></div></td>`;
    tbody.appendChild(er);
  });
  lucide.createIcons();
}

function buildLeft(s) {
  // per-student 8-node timeline
  let tl = '<div class="student-timeline">';
  for (let i=0;i<8;i++) {
    const wn=i+1;
    const done=wn<s.week, current=wn===s.week, upcoming=wn>s.week;
    const connCls = done?'done':'upcoming';
    const nodeCls = done?'stl-done':current?'stl-current':'stl-upcoming';
    const lbl = done?`<div class="stl-date">${s.weekDates[i]||''}</div>` : current?`<div class="stl-date current">In Progress</div>` : '';
    tl += `<div class="stl-item">
      <div class="stl-connector ${connCls}"></div>
      <div class="stl-node ${nodeCls}">${done?'<i data-lucide="check" style="width:10px;height:10px;"></i>':wn}</div>
      <div class="stl-label${current?' current':''}">${WEEKS_LIST[i].split(' ').slice(0,3).join(' ')}</div>
      ${lbl}
    </div>`;
  }
  tl += '</div>';

  // week log
  let log = '<div class="week-log">';
  for (let i=0;i<8;i++) {
    const wn=i+1;
    const done=wn<s.week, current=wn===s.week, upcoming=wn>s.week;
    const sc = s.scores.find(x=>x.w===wn);
    log += `<div class="week-log-item ${done?'done':current?'current':'upcoming'}">
      <div class="wl-num">Week ${wn}</div>
      <div style="flex:1;">
        <div class="wl-topic">${WEEKS_LIST[i]}</div>
        <div class="wl-meta">${done?`Completed ${s.weekDates[i]||''} · Score: ${sc?sc.s+'/10':'—'}${sc?' · '+sc.n:''}`:current?'Currently in progress':'Not started yet'}</div>
      </div>
      <div class="wl-actions">
        ${current?`<button class="mark-done-btn" onclick="event.stopPropagation();markDone(${s.id},${wn})">✓ Mark Complete</button>`:''}
        ${upcoming&&wn===s.week+1?`<button class="mark-done-btn" style="opacity:.5;" onclick="event.stopPropagation();">Start Week</button>`:''}
      </div>
    </div>`;
  }
  log += '</div>';

  // photos
  const photoLabels=['Week 3 Build','Week 4 Sensor','Week 5 CAD','Week 6 Assembly'];
  const colors=['#262626','#1a1a3e','#1e3a1e','#3a1a1a'];
  let photos='<div class="photo-grid">';
  for(let i=0;i<4;i++){
    if(i<s.photos){photos+=`<div class="photo-card filled" style="background:${colors[i]};"><i data-lucide="camera" style="width:16px;height:16px;color:#A3A3A3;"></i><div class="photo-label">${photoLabels[i]}</div></div>`;}
    else{photos+=`<div class="photo-card"><i data-lucide="upload" style="width:14px;height:14px;"></i><span>Upload</span></div>`;}
  }
  photos+='</div>';

  // notes
  let noteHtml = s.notes.map(n=>`<div class="note-item">
    <div class="note-avt">${n.i}</div>
    <div style="flex:1;"><div><span class="note-author">Coach ${n.a}</span><span class="note-date">${n.t}</span></div>
    <div class="note-text">${n.txt}</div></div>
  </div>`).join('');

  return `<div class="panel-left">
    <div class="panel-section-title">Individual Progress — ${s.name.split(' ')[0]}'s Journey</div>
    ${tl}
    <div class="panel-section-title">Week Log</div>
    ${log}
    <div class="panel-section-title" style="margin-top:16px;">Project Photos</div>
    ${photos}
    <div class="panel-section-title">Coach Notes</div>
    <div>${noteHtml}</div>
    <div class="note-add">
      <textarea rows="2" placeholder="Add a note about ${s.name.split(' ')[0]}…"></textarea>
      <button class="btn btn-yellow" style="flex-shrink:0;align-self:flex-end;"><i data-lucide="send" style="width:13px;height:13px;"></i> Post</button>
    </div>
  </div>`;
}

function buildRight(s) {
  const attended = s.attendance.filter(Boolean).length;
  const total = s.scores.length;
  const avg = total?(s.scores.reduce((a,b)=>a+b.s,0)/total).toFixed(1):'—';
  let scoreRows = s.scores.map(sc=>`<tr><td>Wk ${sc.w}</td><td style="font-weight:700;">${sc.s}/10</td><td style="color:var(--gray-1);">${sc.n}</td></tr>`).join('');

  return `<div class="panel-right">
    <div>
      <div class="panel-section-title">Student Stats</div>
      <div class="mini-stat-card">
        <div class="ms-row"><span class="ms-label">Current Week</span><span class="ms-val">Week ${s.week} of 8</span></div>
        <div class="ms-row"><span class="ms-label">Completion</span><span class="ms-val" style="color:${pctColor(s.pct)};">${s.pct}%</span></div>
        <div class="ms-row"><span class="ms-label">Sessions Attended</span><span class="ms-val">${attended} of ${total}</span></div>
        <div class="ms-row"><span class="ms-label">Avg Score</span><span class="ms-val">${avg}/10</span></div>
        <div class="ms-row"><span class="ms-label">Last Activity</span><span class="ms-val" style="${s.stalled?'color:#DC2626;':''}">${s.lastSeen}</span></div>
        <div class="ms-row"><span class="ms-label">Parent Email</span><span class="ms-val" style="font-size:11px;">${s.email.replace('@club.org','@gmail.com')}</span></div>
      </div>
    </div>
    <div>
      <div class="panel-section-title">Weekly Scores</div>
      <table class="score-table">
        <thead><tr><th>Week</th><th>Score</th><th>Note</th></tr></thead>
        <tbody>${scoreRows}</tbody>
      </table>
    </div>
    <div class="parent-report-card">
      <div class="pr-title"><i data-lucide="file-text" style="width:13px;height:13px;display:inline;vertical-align:-2px;margin-right:4px;"></i>Parent Report</div>
      <div class="pr-desc">Generate a PDF showing ${s.name.split(' ')[0]}'s individual progress, attendance, scores, and coach notes.</div>
      <button class="btn btn-yellow" style="width:100%;justify-content:center;" onclick="downloadReport(${s.id})">
        <i data-lucide="download" style="width:13px;height:13px;"></i> Download PDF
      </button>
      <div class="pr-last">Last updated: ${s.lastSeen}</div>
    </div>
  </div>`;
}

function toggleExpand(id) {
  const er = document.getElementById('expand-' + id);
  const btn = document.getElementById('btn-' + id);
  const mr = document.getElementById('row-' + id);
  if (openRow && openRow !== id) {
    document.getElementById('expand-'+openRow).style.display='none';
    const pb=document.getElementById('btn-'+openRow);
    if(pb){pb.classList.remove('open');}
    document.getElementById('row-'+openRow).classList.remove('expanded');
  }
  if (er.style.display==='none') {
    er.style.display=''; btn.classList.add('open'); mr.classList.add('expanded'); openRow=id;
  } else {
    er.style.display='none'; btn.classList.remove('open'); mr.classList.remove('expanded'); openRow=null;
  }
  lucide.createIcons();
}

function markDone(studentId, weekNum) {
  const s = STUDENTS.find(x=>x.id===studentId);
  if (!s) return;
  s.week = weekNum + 1;
  s.pct = Math.min(100, Math.round((weekNum/8)*100));
  s.lastSeen = 'Today';
  s.stalled = false;
  if (weekNum === 8) { s.status = 'Excellent'; s.pct = 100; }
  applyFilters();
  if (openRow === studentId) { setTimeout(() => toggleExpand(studentId), 50); }
}

function applyFilters() {
  const search = document.getElementById('search-input').value.toLowerCase();
  const status = document.getElementById('filter-status').value;
  const week   = document.getElementById('filter-week').value;
  filtered = STUDENTS.filter(s => {
    if (search && !s.name.toLowerCase().includes(search)) return false;
    if (status && s.status !== status) return false;
    if (week && s.week !== parseInt(week)) return false;
    if (showStalled && !s.stalled) return false;
    return true;
  });
  sortFiltered();
  openRow = null;
  renderTable();
}

function toggleStalled() {
  showStalled = !showStalled;
  document.getElementById('stalled-btn').classList.toggle('active', showStalled);
  applyFilters();
}

function sortBy(key) {
  if (currentSort.key===key) currentSort.dir = currentSort.dir==='asc'?'desc':'asc';
  else currentSort = {key, dir:'desc'};
  ['name','week','pct','lastSeen'].forEach(k=>{const el=document.getElementById('si-'+k);if(el)el.textContent='';});
  const el=document.getElementById('si-'+key);
  if(el) el.textContent = currentSort.dir==='asc'?' ↑':' ↓';
  sortFiltered(); openRow=null; renderTable();
}

function sortFiltered() {
  filtered.sort((a,b)=>{
    let av,bv;
    if(currentSort.key==='name'){av=a.name;bv=b.name;}
    else if(currentSort.key==='week'){av=a.week;bv=b.week;}
    else if(currentSort.key==='pct'){av=a.pct;bv=b.pct;}
    else if(currentSort.key==='lastSeen'){av=a.lastSeen;bv=b.lastSeen;}
    if(av<bv) return currentSort.dir==='asc'?-1:1;
    if(av>bv) return currentSort.dir==='asc'?1:-1;
    return 0;
  });
}

function downloadReport(id) {
  const s = STUDENTS.find(x=>x.id===id);
  if(!s) return;
  const attended=s.attendance.filter(Boolean).length;
  const avg=s.scores.length?(s.scores.reduce((a,b)=>a+b.s,0)/s.scores.length).toFixed(1):'—';
  let weekCells='';
  for(let i=0;i<8;i++){const wn=i+1;const done=wn<s.week,current=wn===s.week;weekCells+=`<div class="print-week-cell ${done?'done':''}">${wn}. ${WEEKS_LIST[i]}<br><span style="font-size:10px;color:#737373;">${done?`✓ Completed ${s.weekDates[i]||''}`:current?'In Progress':'Upcoming'}</span></div>`;}
  let scoreRows=s.scores.map(sc=>`<tr><td>Week ${sc.w}</td><td>${sc.s}/10</td><td>Malhar Soni</td><td>${sc.n}</td></tr>`).join('');
  let noteHtml=s.notes.map(n=>`<p style="font-size:12px;margin:6px 0;"><strong>${n.t} — Coach ${n.a}:</strong> ${n.txt}</p>`).join('');
  document.getElementById('print-area').innerHTML=`
    <div class="print-header"><div><div class="print-logo">CAUTION TAPE <span>ROBOTICS</span></div><div class="print-subtitle">Foundation Program · Individual Progress Report · Fall 2025</div></div></div>
    <div class="print-section"><div class="print-section-title">Student</div>
      <div class="print-row"><span class="print-label">Name</span><span class="print-val">${s.name}</span></div>
      <div class="print-row"><span class="print-label">Current Week</span><span class="print-val">Week ${s.week} of 8</span></div>
      <div class="print-row"><span class="print-label">Completion</span><span class="print-val">${s.pct}%</span></div>
      <div class="print-row"><span class="print-label">Status</span><span class="print-val">${s.status}</span></div>
      <div class="print-row"><span class="print-label">Sessions Attended</span><span class="print-val">${attended} of ${s.scores.length}</span></div>
      <div class="print-row"><span class="print-label">Avg Score</span><span class="print-val">${avg}/10</span></div>
    </div>
    <div class="print-section"><div class="print-section-title">Weekly Progress</div><div class="print-week-grid">${weekCells}</div></div>
    <div class="print-section"><div class="print-section-title">Scores</div><table class="print-score-table"><thead><tr><th>Week</th><th>Score</th><th>Coach</th><th>Notes</th></tr></thead><tbody>${scoreRows}</tbody></table></div>
    <div class="print-section"><div class="print-section-title">Coach Notes</div>${noteHtml}</div>
    <div class="print-footer"><span>Generated ${new Date().toLocaleDateString()}</span><span>Caution Tape Robotics Club — Foundation Program</span></div>`;
  const ps=document.createElement('style');
  ps.textContent=`@media print{body .main,body .sidebar{display:none!important;}body #print-area{display:block!important;}}`;
  document.head.appendChild(ps);
  window.print();
  setTimeout(()=>ps.remove(),1500);
}

applyFilters();

/* ── ENROLL STUDENT MODAL ─────────────────────────── */
function openEnrollModal() {
  document.getElementById('enroll-backdrop').style.display = 'flex';
  lucide.createIcons();
  setTimeout(() => document.getElementById('en-name').focus(), 50);
}
function closeEnrollModal(e) {
  if (e && e.target !== document.getElementById('enroll-backdrop')) return;
  document.getElementById('enroll-backdrop').style.display = 'none';
}
function submitEnroll() {
  const name = document.getElementById('en-name').value.trim();
  const grade = document.getElementById('en-grade').value;
  if (!name) {
    document.getElementById('en-name').style.borderColor = '#EF4444';
    document.getElementById('en-name').focus();
    return;
  }
  document.getElementById('enroll-backdrop').style.display = 'none';
  document.getElementById('en-name').value = '';
  document.getElementById('en-grade').value = '9';
  // Add new row to STUDENTS array and re-render
  const initials = name.split(' ').map(w=>w[0]).join('').toUpperCase().slice(0,2);
  const newId = 'f' + Date.now();
  STUDENTS.push({ id:newId, name, grade, week1:null,week2:null,week3:null,week4:null,week5:null,week6:null,week7:null,week8:null,scores:[],notes:[] });
  applyFilters();
  // Toast
  const t = document.getElementById('fn-toast');
  if (t) {
    t.style.display = 'flex';
    t.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#F5D000" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>&nbsp;' + name + ' enrolled in Foundation';
    setTimeout(() => { t.style.display = 'none'; }, 3200);
  }
}
document.addEventListener('keydown', e => {
  if (e.key === 'Escape') document.getElementById('enroll-backdrop').style.display = 'none';
});

/* ── NOTIFICATIONS ────────────────────────────────── */
const NOTIFS = [
  { id:1, unread:true,  icon:'alert-circle',   color:'#EF4444', title:'Overdue Task', body:'Robot chassis CAD is 2 days overdue — Team 42A', time:'2m ago' },
  { id:2, unread:true,  icon:'package',        color:'#F59E0B', title:'Low Inventory', body:'8020 Aluminum Extrusion is low stock (qty: 2)', time:'14m ago' },
  { id:3, unread:true,  icon:'users',          color:'#6366F1', title:'New Student', body:'Jordan Lee enrolled in Foundation Program', time:'1h ago' },
  { id:4, unread:false, icon:'trophy',         color:'#F5D000', title:'Competition Result', body:'Team 42A placed 3rd at State Qualifier', time:'Yesterday' },
  { id:5, unread:false, icon:'check-circle-2', color:'#15803D', title:'Task Completed', body:'Electronics wiring completed by Sam Chen', time:'Yesterday' },
  { id:6, unread:false, icon:'calendar',       color:'#0EA5E9', title:'Upcoming Event', body:'Regional Championship in 12 days — Feb 28', time:'2d ago' },
];

function renderNotifs() {
  const list = document.getElementById('notif-list');
  if (!list) return;
  list.innerHTML = NOTIFS.map(n => `
    <div style="display:flex;gap:12px;padding:12px 20px;cursor:pointer;background:${n.unread ? '#FAFAFA' : 'transparent'};border-left:3px solid ${n.unread ? 'var(--yellow)' : 'transparent'};transition:background .12s;" onmouseover="this.style.background='var(--gray-4)'" onmouseout="this.style.background='${n.unread ? '#FAFAFA' : 'transparent'}';" onclick="dismissNotif(${n.id},this)">
      <div style="width:34px;height:34px;border-radius:50%;background:${n.color}22;flex-shrink:0;display:flex;align-items:center;justify-content:center;">
        <i data-lucide="${n.icon}" style="width:15px;height:15px;color:${n.color};"></i>
      </div>
      <div style="flex:1;min-width:0;">
        <div style="font-size:13px;font-weight:${n.unread ? '700' : '500'};color:var(--black);margin-bottom:2px;">${n.title}</div>
        <div style="font-size:12px;color:var(--gray-1);line-height:1.4;margin-bottom:4px;">${n.body}</div>
        <div style="font-size:11px;color:var(--gray-2);">${n.time}</div>
      </div>
    </div>`).join('');
  lucide.createIcons();
}

function dismissNotif(id, el) {
  const idx = NOTIFS.findIndex(n => n.id === id);
  if (idx > -1) { NOTIFS[idx].unread = false; }
  el.style.borderLeftColor = 'transparent';
  el.style.background = 'transparent';
  el.querySelector('div[style*="font-weight"]').style.fontWeight = '500';
  updateBadge();
}

function markAllRead() {
  NOTIFS.forEach(n => n.unread = false);
  renderNotifs();
  updateBadge();
}

function updateBadge() {
  const dot = document.querySelector('.notif-dot');
  if (!dot) return;
  dot.style.display = NOTIFS.some(n => n.unread) ? '' : 'none';
}

function openNotifPanel() {
  const overlay = document.getElementById('notif-overlay');
  const panel   = document.getElementById('notif-panel');
  overlay.style.display = 'block';
  renderNotifs();
  setTimeout(() => { panel.style.transform = 'translateX(0)'; }, 10);
}

function closeNotifPanel(e) {
  if (e && e.target !== document.getElementById('notif-overlay')) return;
  const panel = document.getElementById('notif-panel');
  panel.style.transform = 'translateX(100%)';
  setTimeout(() => { document.getElementById('notif-overlay').style.display = 'none'; }, 230);
}

document.addEventListener('keydown', e => {
  if (e.key === 'Escape') closeNotifPanel(null);
});
</script>

<!-- Notification Panel -->
<div id="notif-overlay" style="display:none;position:fixed;inset:0;z-index:600;" onclick="closeNotifPanel(event)">
  <div id="notif-panel" style="position:absolute;top:0;right:0;width:340px;height:100%;background:var(--white);box-shadow:-4px 0 24px rgba(0,0,0,.14);display:flex;flex-direction:column;transform:translateX(100%);transition:transform .22s ease;" onclick="event.stopPropagation()">
    <div style="display:flex;align-items:center;justify-content:space-between;padding:20px 20px 16px;border-bottom:1px solid var(--gray-3);">
      <span style="font-size:15px;font-weight:700;color:var(--black);">Notifications</span>
      <div style="display:flex;gap:8px;align-items:center;">
        <button onclick="markAllRead()" style="font-size:11px;font-weight:600;color:var(--gray-1);background:none;border:none;cursor:pointer;padding:4px 8px;border-radius:4px;" onmouseover="this.style.background='var(--gray-4)'" onmouseout="this.style.background='none'">Mark all read</button>
        <button onclick="closeNotifPanel(null)" style="background:none;border:none;cursor:pointer;color:var(--gray-2);font-size:20px;line-height:1;">&#x2715;</button>
      </div>
    </div>
    <div id="notif-list" style="flex:1;overflow-y:auto;padding:8px 0;"></div>
    <div style="padding:14px 20px;border-top:1px solid var(--gray-3);">
      <a href="#" style="font-size:12.5px;font-weight:600;color:var(--gray-1);text-decoration:none;">View all activity</a>
    </div>
  </div>
</div>

<!-- ENROLL MODAL -->
<div id="enroll-backdrop" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,.45);z-index:500;align-items:center;justify-content:center;" onclick="closeEnrollModal(event)">
  <div style="background:var(--white);border-radius:var(--radius);box-shadow:0 16px 48px rgba(0,0,0,.18);width:100%;max-width:400px;overflow:hidden;" onclick="event.stopPropagation()">
    <div style="padding:18px 20px;border-bottom:1px solid var(--gray-3);display:flex;align-items:center;justify-content:space-between;">
      <div style="font-size:15px;font-weight:800;">Enroll Student</div>
      <button onclick="closeEnrollModal()" style="background:none;border:none;cursor:pointer;color:var(--gray-1);display:flex;padding:4px;"><i data-lucide="x" style="width:16px;height:16px;"></i></button>
    </div>
    <div style="padding:18px 20px;display:flex;flex-direction:column;gap:14px;">
      <div>
        <label style="font-size:11px;font-weight:700;color:var(--gray-1);text-transform:uppercase;letter-spacing:.06em;display:block;margin-bottom:5px;">Full Name *</label>
        <input id="en-name" type="text" placeholder="e.g. Sam Johnson" style="width:100%;border:1.5px solid var(--gray-3);border-radius:var(--radius-sm);padding:8px 12px;font-size:13px;font-family:'Inter',sans-serif;color:var(--black);outline:none;" onfocus="this.style.borderColor='var(--yellow)'" onblur="this.style.borderColor='var(--gray-3)'"/>
      </div>
      <div>
        <label style="font-size:11px;font-weight:700;color:var(--gray-1);text-transform:uppercase;letter-spacing:.06em;display:block;margin-bottom:5px;">Grade</label>
        <select id="en-grade" style="width:100%;border:1.5px solid var(--gray-3);border-radius:var(--radius-sm);padding:8px 12px;font-size:13px;font-family:'Inter',sans-serif;color:var(--black);outline:none;background:var(--white);">
          <option value="9">9th Grade</option>
          <option value="10">10th Grade</option>
          <option value="11">11th Grade</option>
          <option value="12">12th Grade</option>
        </select>
      </div>
      <div>
        <label style="font-size:11px;font-weight:700;color:var(--gray-1);text-transform:uppercase;letter-spacing:.06em;display:block;margin-bottom:5px;">Season</label>
        <select style="width:100%;border:1.5px solid var(--gray-3);border-radius:var(--radius-sm);padding:8px 12px;font-size:13px;font-family:'Inter',sans-serif;color:var(--black);outline:none;background:var(--white);">
          <option>Fall 2025</option>
          <option>Spring 2026</option>
        </select>
      </div>
    </div>
    <div style="padding:14px 20px;border-top:1px solid var(--gray-3);display:flex;gap:8px;justify-content:flex-end;">
      <button class="btn btn-outline" onclick="closeEnrollModal()">Cancel</button>
      <button class="btn btn-yellow" onclick="submitEnroll()"><i data-lucide="user-plus" style="width:13px;height:13px;"></i> Enroll</button>
    </div>
  </div>
</div>

<!-- TOAST -->
<div id="fn-toast" style="display:none;position:fixed;bottom:24px;right:24px;background:var(--black);color:var(--white);padding:12px 18px;border-radius:var(--radius-sm);font-size:13px;font-weight:600;z-index:600;box-shadow:0 4px 16px rgba(0,0,0,.2);align-items:center;gap:8px;"></div>

</body>
</html>
